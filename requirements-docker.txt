# CPU-only dependencies for production Docker inference.
# Versions are pinned for deterministic builds and stability across machines.
ultralytics==8.2.0
paddleocr==2.8.0
paddlepaddle==2.6.2
opencv-python-headless==4.10.0.84
pillow==12.2.0
numpy==1.26.4
requests==2.31.0
PyYAML==6.0.1
